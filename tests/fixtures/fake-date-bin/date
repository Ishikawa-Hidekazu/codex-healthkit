#!/usr/bin/env bash
set -euo pipefail

if [ "${1:-}" = "-u" ] && [ "${2:-}" = "+%Y-%m-%dT%H:%M:%SZ" ] && [ "$#" -eq 2 ]; then
  printf '%s\n' "${FAKE_DATE_NOW:-2026-08-02T00:00:00Z}"
  exit 0
fi

exec /bin/date "$@"
